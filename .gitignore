node_modules/
out/
dist/
release/
*.log
.DS_Store
.vite/

# tsc incremental build info (composite projects)
*.tsbuildinfo

# Smoke-test artifacts (scripts/smoke.cjs)
smoke-out/
smoke-stdout.txt

# yarn is the package manager for this project; npm's lockfile must not come back
package-lock.json
